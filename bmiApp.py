import streamlit as st

# ส่วนที่ 1 หัวข้อหน้าเว็บ (Title สีแดงสดใส)
st.markdown("# :red[🏋️✨ คำนวณค่าดัชนีมวลกาย BMI 💖]")
st.write("🏃‍♂️ กรอกข้อมูลน้ำหนักและส่วนสูงของคุณ เพื่อเช็กสุขภาพเบื้องต้นกันเถอะ 🍎")

# ส่วนที่ 2 สร้างช่องรับค่าน้ำหนัก และ ส่วนสูง
weight = st.number_input("⚖️ กรอกน้ำหนักของคุณ (กิโลกรัม):", min_value=1.0)
height_cm = st.number_input("📏 กรอกส่วนสูงของคุณ (เซนติเมตร):", min_value=1.0)

# ส่วนที่ 3 สร้างปุ่มกดคำนวณ
if st.button("คำนวณค่า BMI 🎯✨"):
    # แปลงส่วนสูงจาก cm เป็น เมตร แล้วคำนวณ BMI
    height_m = height_cm / 100
    bmi = weight / (height_m ** 2)
    
    st.write("---")
    st.header(f"💪 ค่า BMI ของคุณคือ: **{bmi:.2f}** 🩺")

    # ส่วนที่ 4 แปลผลค่า BMI ตามเกณฑ์
    if bmi < 18.5:
        st.warning("⚠️ คุณมีน้ำหนักน้อยกว่าเกณฑ์ (ผอม) 🍎 อย่าลืมทานอาหารให้ครบ 5 หมู่นะครับ")
    elif 18.5 <= bmi < 23.0:
        st.success("🎉 คุณมีน้ำหนักอยู่ในเกณฑ์ปกติ (สุขภาพดี) 🥗 สุขภาพแข็งแรงมากๆ สู้ๆ!")
    elif 23.0 <= bmi < 25.0:
        st.info("💡 คุณเริ่มมีน้ำหนักเกินเกณฑ์ (ท้วม) 🏃‍♂️ ลองออกกำลังกายวันละนิดนะ")
    else:
        st.error("🚨 คุณอยู่ในเกณฑ์อ้วน 🔥 ควรระวังเรื่องสุขภาพและเริ่มออกกำลังกายอย่างสม่ำเสมอนะครับ")

st.divider()
st.write("⭐ นางสาวกนกพิชญ์ วชิรโกวิทย์ เลขที่ 23 ม.4/3")
